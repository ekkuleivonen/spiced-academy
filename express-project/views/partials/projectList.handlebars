<h1>WELCOME TO MY PORTFOLIO</h1>
<main>

    {{#projects}}
        <a href="preview/{{directory}}"">
            <div class="project">
                <h2>{{title}}</h2>
                <img src="{{image}}" alt="project screenshot" />
            </div>
        </a>

    {{/projects}}
</main>